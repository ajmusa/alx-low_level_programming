#include "main.h"
/**
 * add - adds two integers.and returns the result
 *
 * main - prints the result of the addition of two integers
 *
 *@n: keeps track of one integer.
 *@o: keeps track of the second integer.
 *
 * Return: a single output of addition of the two digits
 */

int add(int n, int o)
{
	return (n + o);
}
