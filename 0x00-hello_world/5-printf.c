#include <stdio.h>
/**
 * main - Uses printf to print out text followed by a new line
 * Return: Always value of 0
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
