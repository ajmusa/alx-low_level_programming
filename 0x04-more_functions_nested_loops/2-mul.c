#include "main.h"
/**
 *mul - a function that multiplies two integers together
 *main - multiplies two integers together
 *@a: stores one integer number
 *@b: stores a second integer number
 *Return: returns value of a * b integers
 */

int mul(int a, int b)
{
	return (a * b);
}
